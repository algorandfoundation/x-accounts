name: Release

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - '**.md'
      - '.vscode/**'
      - '.idea/**'

permissions:
  contents: read
  packages: read

jobs:
  logicsig-validate:
    name: Run logicsig release
    secrets: inherit
    uses: ./.github/workflows/logicsig-ci.yaml

  logicsig-release:
    name: Run logicsig release
    secrets: inherit
    uses: ./.github/workflows/logicsig-cd.yaml
    needs: logicsig-validate
